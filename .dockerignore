# 构建上下文里不需要的东西。少传一点，构建快一点，也少一份泄露风险。
#
# 两条最要紧的排除，理由都不是「省空间」：
#   .env.local —— 里面是三个真的 Key，进了镜像就是进了 Docker Hub
#   data/      —— 真实库里有行程与结论，那是私人数据
#
# 镜像要用的那份资料（车站名表）已经挪到 lushu/seed/ 跟着版本库走，
# data/ 下不再有任何构建需要的东西，所以整目录排掉，不留例外。

# ─── 版本库与编辑器 ─────────────────────────────────────────────
.git
.gitignore
.gitattributes
.vscode
.idea
*.swp
.DS_Store
Thumbs.db

# ─── 密钥与环境文件 ─────────────────────────────────────────────
.env
.env.local
.env.*.local

# ─── Python ─────────────────────────────────────────────────────
.venv
.pytest_cache
.ruff_cache
**/__pycache__
**/*.py[cod]
lushu.egg-info

# ─── 前端 ───────────────────────────────────────────────────────
# node_modules 必须排掉：宿主机是 Windows，装的是 win32 二进制，
# 拷进 Linux 构建镜像里 esbuild 会以「找不到平台包」的形式炸掉。
web/node_modules
# dist 由第一段重新构建，本地那份不参与，免得旧产物混进镜像
web/dist
web/.vite

# ─── 运行期数据 ─────────────────────────────────────────────────
# 真实库、素材底档、导出的路书、规则草稿，一个都不该进镜像。
data/

# ─── 调研草稿与一次性脚本 ───────────────────────────────────────
_raw
tools
scripts
.shots
tests
docs
CONTEXT.md
